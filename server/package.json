{
  "name": "bravevoice-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Receives BraveVoice session data from the app and writes it to each therapist's Google Sheet.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "googleapis": "^144.0.0",
    "helmet": "^8.0.0"
  }
}
