# Base URL for the BraveVoice sync backend (see /server). Public because it's bundled
# into the client — never put secrets in EXPO_PUBLIC_ variables.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000/v1
